import { JSX } from "react"
import { Redirect } from "expo-router"

const Index = (): JSX.Element => {
  // return <Redirect href='bell/bell' />
  return <Redirect href='bell/bellSelection' />
}

export default Index
